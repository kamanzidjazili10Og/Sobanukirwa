# Render Environment Variables
# Set these in Render Dashboard > Environment tab
# These are for LOCAL development only - Render uses its own env vars

PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=sobanukirwa
DB_PORT=3306
JWT_SECRET=your_jwt_secret_here
ADMIN_PASSWORD=Sobanukirwa@123
